import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container:{

    },
    font: {
        textAlign: 'center',
        fontSize: 24,
        fontWeight: 'bold',
    }
})

export { styles }